<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Student Detail</title>
    <link rel="stylesheet" href="student.css">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap" rel="stylesheet">
</head>
<body>
    <div class="bg-shape shape1"></div>
    <div class="bg-shape shape2"></div>
    <div class="bg-shape shape3"></div>

    <div class="container">
        <a href="/students" class="back-btn">
            ← Back to Students
        </a>

        <div class="profile-card">
            <%const {roll_no,name,email,gender,state,city,pincode,course,contact_no,admission_date,address} = results[0];%>
            
            <div class="profile-header">
                <div class="profile-icon">👨‍🎓</div>
                <h1><%=name%></h1>
                <p class="roll-number">Roll No: <%=roll_no%></p>
            </div>

            <div class="profile-body">
                <h2 class="section-title">📋 Personal Information</h2>
                
                <div class="info-grid">
                    <div class="info-item">
                        <div class="info-label">📧 Email</div>
                        <div class="info-value"><%=email%></div>
                    </div>

                    <div class="info-item">
                        <div class="info-label">⚧ Gender</div>
                        <div class="info-value">
                            <span class="gender-badge gender-<%=gender.toLowerCase()%>">
                                <%=gender%>
                            </span>
                        </div>
                    </div>

                    <div class="info-item">
                        <div class="info-label">📞 Contact Number</div>
                        <div class="info-value"><%=contact_no%></div>
                    </div>

                    <div class="info-item">
                        <div class="info-label">📚 Course</div>
                        <div class="info-value"><%=course%></div>
                    </div>

                    <div class="info-item">
                        <div class="info-label">📅 Admission Date</div>
                        <div class="info-value"><%=admission_date%></div>
                    </div>

                    <div class="info-item">
                        <div class="info-label">🏙️ City</div>
                        <div class="info-value"><%=city%></div>
                    </div>

                    <div class="info-item">
                        <div class="info-label">🗺️ State</div>
                        <div class="info-value"><%=state%></div>
                    </div>

                    <div class="info-item">
                        <div class="info-label">📮 Pincode</div>
                        <div class="info-value"><%=pincode%></div>
                    </div>
                </div>

                <div class="address-section">
                    <div class="info-label">🏠 Full Address</div>
                    <div class="info-value"><%=address%></div>
                </div>

                <div class="action-buttons">
                    <a href="/edit-student?id=<%=roll_no%>" class="btn btn-edit">
                        ✏️ Edit Details
                    </a>
                    <button onclick="deleteStudent(<%=roll_no%>)" class="btn btn-delete">
                        🗑️ Delete Student
                    </button>
                </div>
            </div>
        </div>
    </div>

    <script>
        function deleteStudent(rollNo) {
            if (confirm('⚠️ Are you sure you want to delete this student? This action cannot be undone.')) {
                window.location.href = `/delete-student?id=${rollNo}`;
            }
        }
    </script>
</body>
</html>